import {modeGenerator} from "@atlaskit/navigation-next";
import {colors} from "@atlaskit/theme";

export const navTheme = modeGenerator({
    product: {
        text: colors.N0,
        background: "#343482",
    },
});
